enum FeatureFlags {
    /// Set to true to show the Reminders tab (Epic 12). When false, the tab is hidden.
    static let reminders: Bool = true

    /// Set to true to show the Tasks tab (Epic 13). When false, the tab is hidden.
    static let tasks: Bool = true
}
